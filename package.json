{
  "type": "module",
  "scripts": {
    "dev": "pnpm -C explorer exec vite",
    "pretest": "esbuild ./test/index.js --outdir=dist/test --bundle --platform=node --format=esm --packages=external --sourcemap",
    "test": "node ./dist/test/index.js",
    "lint": "dprint check",
    "lint:fix": "dprint fmt"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "dprint": "^0.50.1",
    "esbuild": "^0.25.8"
  }
}
