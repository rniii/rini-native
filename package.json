{
  "type": "module",
  "scripts": {
    "test": "esbuild ./test --bundle --platform=node --format=esm --sourcemap=inline | node --enable-source-maps -",
    "lint": "dprint check",
    "lint:fix": "dprint fmt"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "dprint": "^0.50.1",
    "esbuild": "^0.25.8"
  }
}
