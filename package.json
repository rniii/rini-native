{
  "type": "module",
  "scripts": {
    "dev": "pnpm -C explorer exec vite",
    "test": "node ./test/index.ts",
    "lint": "dprint check",
    "lint:fix": "dprint fmt"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "dprint": "^0.50.1",
    "esbuild": "^0.25.8"
  },
  "dependencies": {
    "decompiler": "workspace:^"
  }
}
